{
  "name": "abhinav-portfolio",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "npx tailwindcss -i ./src/input.css -o ./src/output.css --watch & python -m http.server 3000 --directory ./src",
    "build": "npx tailwindcss -i ./src/input.css -o ./src/output.css --minify",
    "start": "python -m http.server 3000 --directory ./src",
    "serve": "npx serve ./src -p 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Abhinav Kumar Jha",
  "license": "ISC",
  "description": "A responsive and modern personal portfolio site",
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.6",
    "tailwindcss": "^4.0.6"
  }
}
